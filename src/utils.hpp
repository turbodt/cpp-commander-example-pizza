#ifndef H_INCLUDE_CLI_UTILS
#define H_INCLUDE_CLI_UTILS

#include <algorithm>
#include <string>

extern std::string to_lower(std::string const &);
extern std::string capitalize(std::string const &);

#endif
